events {
  worker_connections 1024;
}

http {
  # Upstream to the Node app
  upstream app_upstream {
    server app:3000;
    keepalive 32;
  }

  # ----- HTTP server (port 80) -----
  server {
    listen 80;
    server_name _;

    # Option A (active): proxy directly to app over HTTP
    location / {
      proxy_pass http://app_upstream;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Option B (commented): force HTTPS with 301 redirect
    # return 301 https://$host$request_uri;
  }

  # ----- HTTPS server (port 443) -----
  # server {
  #   listen 443 ssl;
  #   server_name your.domain.example;  # change to your domain
  #
  #   # Place your certs in a mounted folder and point here
  #   ssl_certificate     /etc/ssl/demo/fullchain.pem;
  #   ssl_certificate_key /etc/ssl/demo/privkey.pem;
  #
  #   location / {
  #     proxy_pass http://app_upstream;
  #     proxy_set_header Host $host;
  #     proxy_set_header X-Real-IP $remote_addr;
  #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #     proxy_set_header X-Forwarded-Proto $scheme;
  #   }
  # }
}
